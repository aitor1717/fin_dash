.venv/
__pycache__/
*.pyc

# Ad-hoc screenshots dropped at the project root for visual QA during a
# session -- not source, not curated references (those live in inspo/).
/Screenshot_*.png

# Real trade history and anything built from it -- only feed/sample_feed.csv
# (fully synthetic, see pipeline/generate_sample_feed.py) and its own
# docs/data-sample/ output are meant to be public. Everything below is
# either the real feed or a config/output derived from it.
feed/example_feed*.csv
feed/example_feed*.csv.bak
feed/hypothetical_augmented.csv
config.yaml
config.feed2.yaml
config.tolerant15.yaml
config.w15.yaml
config.w20.yaml
config.halved*.yaml
config.augmented.yaml
docs/data/
docs/data-augmented/
docs/data-feed2/
docs/data-tolerant15/
docs/data-w15/
docs/data-w20/
docs/data-halved*/

# Unrelated personal planning doc that ended up in this working directory --
# not part of the dashboard project, not meant to be public.
/ttp-deployment-runbook.html
/project-pipeline-memoir-fin-dash.html

# Internal working notes, not meant to be public.
/CLAUDE.md

# Design references (mood boards, some sourced from stock/Tumblr images with
# unclear reuse rights) and exploratory mockups superseded by the final
# docs/index.html -- neither is part of the published product.
/inspo/
/docs/mockups/
